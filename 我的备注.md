检查后端服务
netstat -ano | findstr ":8500"

如果后端未启动，请手动启动：
$env:PATH += ";$env:USERPROFILE\.deno\bin"
cd D:\code\weixin\ai-trend-publish
deno task start


3. 启动脚本

start-backend.bat - 后端启动脚本

start-frontend.bat - 前端启动脚本
